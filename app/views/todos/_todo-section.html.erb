<section class="TodoSection">
        <div class="TodoSectionTitle">
          <p class="TodoSectionTitle-text"><%= section_title %></p>
          <p class="TodoSectionTitle-remainingCount"><%= todos.completed.count %>/<%= todos.count %></p>
        </div>
        <ul>
        <% todos.each do |todo| %>
          <li class="TodoItem">
            <div class="TodoItem-container">
              <input class="TodoItem-checkbox" type="checkbox" <%= todo.completed ? "checked" : ""%>>
              <%= todo.todo_text %>
              <% if show_due_date %>
              <p class="TodoItem-dueDate"><%= todo.due_date.to_fs(:short) %></p>
              <% end %>
              <a class="TodoItem-delete" href="">
                <img src=<%= image_url("delete.svg") %>  alt="thrashcan image" />
              </a>
            </div>
          </li>
        <% end %>

        </ul>
      </section>
